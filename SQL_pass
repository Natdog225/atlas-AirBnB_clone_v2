root
NewPassword123!

host: localhost
port: 3302

Use this to log into SQL database:
mysql -u root -p

## Run this command to create the database Dev.
cat setup_mysql_dev.sql | mysql -hlocalhost -uroot -p

## Run this command to create the database test

cat setup_mysql_test.sql | mysql -hlocalhost -uroot -p

it will then prompt for the root password. see above.

need to add to the container. 
python dotenv
python pymysql